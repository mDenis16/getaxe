#include "../features.hpp"
//#include <nlohmann/json.hpp>

//using json = nlohmann::json;
namespace connection {
	
	void telemetry::send ( ) {
	
		/*json data; 

		data [ "id" ] = id;
		data [ "map" ] = map;
		data [ "position_x" ] = position.x;
		data [ "position_y" ] = position.y;
		data [ "position_z" ] = position.z;
		data [ "max_desync_delta" ] = max_desync_delta;
		data [ "lby" ] = lby;
		data [ "autowall_side" ] = autowall_side;
		data [ "hit_side" ] = hit_side;
		data [ "side" ] = side;
		data [ "missed" ] = missed;
		data [ "left_yaw" ] = id;
		data [ "right_yaw" ] = id;
		data [ "yaw" ] = id;
		data [ "goal_feet" ] = goal_feet;

		connection::send ( "pushTelemetry|" + data.dump() );*/

	}
}